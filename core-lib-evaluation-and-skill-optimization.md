# chanlun 核心库能力评估 & chanlun-skill 功能覆盖审计

> **状态说明（2026-09-20）**：本文第 8～10 节保留早期审计基线，部分“未实现”结论已被后续 Skill 改造覆盖。当前实现以 `skills/chanlun-skill/SKILL.md`、`scripts/chan_analyzer.py` 及现行回归测试为准；本文中的历史缺陷数字不应作为当前版本能力判断。

> 审计对象：`chanlun` v2606.73（Rust + PyO3 绑定）、`chanlun-skill` v1.0.0
> 环境：Python 3.14 / `.venv` / Windows
> 方法：静态通读 `__init__.pyi`(893 行) + `chan.py`，配合 4 轮运行时探针（600 根日线合成数据 + 真实 `test_data.csv`）
> 结论均附实测数字，未验证的推断已显式标注

---

## 0. 摘要

**核心库的能力远强于项目当前所用，而项目对官方语义的理解存在方向性错误。**

三句话总结：

1. **核心库是一套完整的缠论引擎**：22 个结构序列（含 3 层扩展/混合级别）、74 个配置字段、18 种买卖点类型、14 个背驰判定方法、完整指标栈。项目实际只用了其中约 **27%** 的结构序列、**0** 种官方买卖点类型。
2. **最严重的不是"没用到"，而是"用反了"**：项目自研的买卖点方向映射与核心库官方语义 **18/18 完全反向**，且同一笔会同时被判为一卖和二买。
3. **Skill 声称的四项核心能力中，两项未实现**（技术指标、线段级分析），一项被阻断（多周期），实际可用的只有结构计数与一份汇总表。

---

## 1. 核心库架构

### 1.1 双层结构

```
chanlun/
├── __init__.py          导出面（Python 层叠加）
├── __init__.pyi         Rust 绑定类型注解（893 行）—— 权威 API 来源
├── _chanlun.cp314.pyd   Rust 扩展（PyO3）
└── chan.py              Python 层（0.3MB）：枚举、配置、相等性、日志
```

**注意**：存在**两个同名 `缠论配置`** 类，容易踩坑。

| 类 | 来源 | 特性 |
|---|---|---|
| `chanlun.缠论配置` | Rust 绑定 | 无 `model_fields()`；项目 `config.py` 用的是这个 |
| `chanlun.chan.缠论配置` | Python 层 | 有 `model_fields()` / `to_dict` / `from_json` / `对比` |

两个类都能被 `立体分析器` 接受（实测均通过）。

### 1.2 模块级全局开关（项目完全未触及）

| 函数 | 当前值 | 作用 |
|---|---|---|
| `get_分型模式()` / `set_分型模式()` | `True` | 切换分型判定模式 |
| `get_扩展线段模式()` / `set_扩展线段模式()` | `True` | 切换扩展线段模式 |
| `get_log_mode()` / `set_log_mode()` | `'off'` | 日志模式 |
| `get_cache_mode()` / `set_cache_mode()` | `'thread_local'` | 缓存模式（影响并发） |
| `set_rs_log_level()` / `get_rs_log_level()` | `'info'` | Rust 侧 tracing 日志 |

`set_cache_mode` 对多线程/多标的批量分析有直接意义，项目未涉及。

---

## 2. 分析器模型：`立体分析器` 的正确用法

这是**最容易踩错**的一点，实测结论如下：

```
立体分析器(符号, 周期组=[小, 中, 大...], 配置)   # 周期组长度必须 ≥ 2
   └─ 只投喂【最小周期】的 K线 → 引擎自动合成所有更大周期
```

| 周期组 | 投喂 | 结果 |
|---|---|---|
| `["day","week"]` | 86400s × 58 根 | day=57，**自动合成 week=12** ✓ |
| `["week","day"]` | 86400s × 58 根 | 同上（引擎自动排序，声明顺序无关）✓ |
| `["day","week","month"]` | 86400s × 58 根 | day=57 / week=12 / month=3 ✓ |
| `["5m","day"]` | 300s × 58 根 | 两周期各 57 根 ✓ |
| `["day"]` **单周期** | — | **构造即 `PanicException`** |
| `["30m","day"]` 却喂 86400s | — | **`PanicException: 周期不匹配 86400 != 1800`** |

**三个必须记住的约束**：

1. **周期组长度必须 ≥ 2**。单周期触发 Rust `index out of bounds: the len is 1 but the index is 1`。
2. **只能投喂最小周期**。`立体分析器.投喂K线()` 校验 K线周期 == 周期组最小值，不匹配即硬崩。
3. **`PanicException` 继承 `BaseException` 而非 `Exception`** —— 所有 `try/except Exception` 都抓不住，进程直接猝死。

绕过周期校验的办法（需要手工控制每个周期时）：直接对**观察者**投喂 —— `analyzer.get_observer(sec).投喂原始数据(...)` 或 `.增加原始K线(...)`。

`立体分析器` 的公开面极窄：`周期组`（只读）、`投喂K线`、`测试_保存数据`；`_单体分析器` 是**私有**字段。

---

## 3. 数据结构：22 个序列，3 层级别

`观察者` 暴露的完整序列清单（`.pyi:787-843`）：

| 分组 | 序列 |
|---|---|
| 基础 | `普通K线序列`、`基础缠K序列`、`缠论K线序列`、`分型序列` |
| 笔级 | `笔序列`、`笔_中枢序列` |
| 线段级 | `线段序列`、`中枢序列` |
| 扩展级 | `扩展线段序列`、`扩展中枢序列`、`扩展线段序列_线段`、`扩展中枢序列_线段` |
| 线段之上的线段 | `线段_线段序列`、`线段_中枢序列`、`扩展线段序列_扩展线段`、`扩展中枢序列_扩展线段` |
| 分组（多层） | `线段序列组`、`中枢序列组`、`扩展线段序列组`、`扩展中枢序列组`、`混合扩展线段序列组`、`混合扩展中枢序列组` |

**600 根日线实测产出**：

```
86400s: 普K=599 缠K=413 分型=31 笔=30 笔中枢=3 线段=5 线段中枢=1
        扩展线段=10 扩展中枢=1
        线段序列组    = [5, 0, 0]
        中枢序列组    = [1, 0, 0]
        扩展线段序列组 = [10, 3, 1]   ← 3 层级别
        混合扩展线段序列组 = [1, 0, 0]
604800s: 普K=86 缠K=65 分型=8 笔=7 笔中枢=1 扩展线段=2
```

**项目覆盖率**：`text.py` 用了 6 / 22 个，且只取 `线段序列组[0]`（第 1 层）。丢掉的正是**扩展线段 / 扩展中枢 / 混合扩展**——即缠论「级别递归」的载体。

---

## 4. 官方买卖点体系

### 4.1 类型枚举：18 种

`一买/一卖`、`二买/二卖`、`三买/三卖`，外加 **T 系列 12 种**：
`T1买/卖`、`T1P买/卖`、`T2买/卖`、`T2S买/卖`、`T3A买/卖`、`T3B买/卖`。
每项带 `是买点` / `是卖点` 属性（实测全部正确）。

项目 `identify_standard_signals()` 只产出 6 种基础类型，**T 系列完全未实现**。

### 4.2 判定入口：`虚线.买卖意义(线, 观察者) -> (bool, str)`

实测 30 个笔命中 **18 个**，返回原因字符串形如：

```
'背驰过:0,极值:True,柱子分型匹配'
'武之MACD均值'
'背驰过:0,极值:False,柱子分型匹配'
```

### 4.3 ⚠️ 语义陷阱：`买卖意义` 不返回"买/卖"

该 API 回答的是**「此处是否具备买卖意义」**，**不告诉你买点还是卖点**，也不返回类型。

项目 `analyzer.get_signals()` 用 `笔.方向` 反推：

```python
is_buy = "向上" in direction      # ← 项目自行发明的映射
```

**实测结论：这个映射是反的。**

验证方法：核心配置注释明确写着 `买卖点_指标匹配_MACD: bool = True  # 买在负，卖在正！`。
逐笔核对 18 个命中样本的终点 MACD 柱符号：

| 笔# | 方向 | 项目判定 | 终点 MACD 柱 | 按官方语义 |
|---|---|---|---|---|
| 0 | 向上 | buy | +0.09564 | **卖点** |
| 1 | 向下 | sell | −0.03090 | **买点** |
| 2 | 向上 | buy | +0.05668 | **卖点** |
| 7 | 向下 | sell | −0.03166 | **买点** |
| 17 | 向下 | sell | −0.17269 | **买点** |
| 29 | 向下 | sell | −0.06738 | **买点** |

> **一致率 = 0 / 18。即 100% 反向。**

交叉验证：向上笔结构为 `文=底 → 武=顶`（末端在顶），向下笔为 `文=顶 → 武=底`（末端在底）。缠论语义下，向下笔在底分型处背驰结束 → **买点**。项目把它标成 sell。

其余 12 个样本同向，无一例外。

### 4.4 `缠论K线.买卖点信息`

该属性在**全部 5 种配置组合**下均返回**空 `set()`**（默认 / `买卖点_指标模式=全量` / `=任意` / `买卖点激进识别=True` / `图表展示+推送全开`）。

**结论**：本绑定版本中它不是可用的买卖点载体。核心库**不提供现成的买卖点序列属性** —— 官方路径是 `买卖意义` + 配置参数 + `买卖点` 工厂类。

### 4.5 `买卖点` 工厂 + `基础买卖点` 字段（项目未用）

`买卖点.一买点/一卖点/二买点/.../T3B卖点`（18 个 classmethod）可构造买卖点对象，实测成功：

```python
bp = 买卖点.一买点(分型, K线, "标识", "备注", 破位值)
# → 一买<sh600000<410, 底, 86400, 相对方向.向上, 1651449600, 10.7595, 10.5161>, 186, -1>
```

可用字段：`类型`、`备注`、`买卖点分型`、`买卖点K线`、`当前K线`、`失效K线`、`终结K线`、`破位值`、`结构`、`偏移`、`失效偏移`、`有效性`、`与MACD柱子匹配`、`与MACD柱子分型匹配`。

其中 **`破位值` / `失效K线` / `失效偏移` / `有效性`** 直接对应缠论的**止损与失效判定**，项目完全没有使用。

---

## 5. 背驰体系

### 5.1 `背驰分析`：14 个 classmethod

| 静态版（需 K线序列） | `_OBS` 版（需 观察者） |
|---|---|
| `MACD背驰`、`斜率背驰`、`测度背驰` | `MACD背驰_OBS` |
| `全量背驰`、`任意背驰`、`任选背驰` | `全量背驰_OBS`、`任意背驰_OBS`、`任选背驰_OBS` |
| `配置背驰`、`背驰模式` | `配置背驰_OBS`、`背驰模式_OBS` |

`_OBS` 变体由观察者自动取序列，**比静态版更省事**，项目用的是静态版。

**实测（线段对 4 组）**：

| 线段对 | MACD背驰 | 斜率背驰 | 测度背驰 | 全量背驰 | 配置背驰 | 任意背驰 |
|---|---|---|---|---|---|---|
| #0→#1 | True | False | False | False | False | True |
| #1→#2 | False | False | False | False | False | False |
| #2→#3 | True | False | False | False | False | True |
| #3→#4 | True | False | False | False | False | True |

**⚠️ 需注意**：本次数据上 **斜率背驰与测度背驰恒为 False**，`全量背驰`/`配置背驰` 也恒为 False。（此现象在单一数据集上得出，是否与数据特征或实现细节有关，**尚需更多样本验证**，不宜草率下结论。）

但 Skill 的 `examples/usage-scenarios.md` 场景 5 直接写「斜率背驰：是 / 测度背驰：待确认」——**该示例输出在当前核心库上无法复现**。

### 5.2 `笔` / `线段` 的类方法（注意调用约定）

**这些都是 `classmethod`，实例要作为第一个参数传入**，不是实例方法：

```python
from chanlun import 笔, 线段
线段.四象(seg)                      # → '老阴' / '小阳' / '少阴'
线段.特征序列状态(seg)               # → (True, False, False)
线段.特征分型终结(seg)               # → bool
线段.获取缺口(seg)                   # → Optional[缺口]
线段.是否背驰过(seg, obs)            # → List[缠论K线]
线段.判断线段内部是否背驰(seg, obs)    # → bool
线段.获取所有停顿位置(seg, obs)       # → List[虚线]
线段.分割序列(seg)                   # → (前, 中, 后, 目标)
线段.获取内部中枢序列(seg, cfg)       # → (List[中枢], List[中枢], List[中枢])

笔.是否背驰过(st, obs)               # → List[缠论K线]
笔.获取所有停顿位置(st, obs)          # → List[虚线]
笔.根据缠K找笔(笔序列, 缠K)           # → Optional[虚线]
笔.以文会友(笔序列, 分型) / 笔.以武会友(笔序列, 分型)
```

实测全部可用。**若按实例方法调用会抛 `AttributeError: '虚线' object has no attribute '四象'`** —— 很容易误判为"核心库没有这个能力"。

`线段.四象` 返回 `老阴/小阳/少阴/...`，`判断线段内部是否背驰` 返回 True/False —— 都是**分级别的背驰判据**，比项目用的"线段对背驰"更贴近缠论原文。

---

## 6. 指标体系

### 6.1 项目 `get_indicators()` 是空的

实测返回：

```python
{'macd': {'fast': 12, 'slow': 26, 'signal': 9},
 'rsi':  {'period': 13, 'overbought': 75.0, 'oversold': 25.0},
 'kdj':  {'rsv_period': 13, 'k_smooth': 5, 'd_smooth': 5, ...}}
```

**这些全是配置参数，不是指标数值。** 也就是说 `chan_analyzer.py --cal_macd` 打印的「MACD: 已计算」是纯字面输出 —— **技术指标功能实际未实现**。

### 6.2 核心库真正的指标接口

指标挂在**每根 K线**上：

```python
K线.macd   → 平滑异同移动平均线(DIF=..., DEA=..., BAR=...)
K线.rsi    → 相对强弱指数(RSI=..., 平均上涨=..., 平均下跌=...)
K线.kdj    → 随机指标(K=..., D=..., J=...)
K线.指标   → 指标容器，keys = ['单值','macd','rsi','kdj','boll','均线']
```

实测数值（600 根日线，末根）：

```
DIF=-0.23152  DEA=-0.17284  MACD柱=-0.05868  快线EMA/慢线EMA 齐全
RSI=35.04759  平均上涨=0.0459  平均下跌=0.0855
KDJ: K=14.04  D=19.18  J=3.75
BOLL: 上=11.65  中=11.17  下=10.69
```

配置 `计算BOLL=True` + `均线_类型列表=['SMA','EMA']` + `均线_周期列表=[5,20]` 后：

```
[均线] = {'EMA_20': 11.0683, 'SMA_5': 10.6835, 'SMA_20': 11.1510, 'EMA_5': 10.7253}
[单值] = {}
```

### 6.3 一个被忽视的强接口：`K线.获取MACD(序列, 始, 终)`

```python
K线.获取MACD(普K序列, 起始K线, 结束K线)
# → {'阴': -13.7135, '阳': 13.0221, '总': 26.7356, '合': -0.6913}
```

返回的是 **MACD 柱面积的分向统计**（阴/阳/总/合）—— 这正是**背驰判定所需的核心量**。项目完全未使用。

---

## 7. 配置面：74 个字段

`chanlun.chan.缠论配置.model_fields()` 实测 **74 个字段**（项目 docstring 声称「60+」）。

按功能分组，项目 `config.py` 的处理情况：

| 分组 | 字段数 | 项目做法 | 评价 |
|---|---|---|---|
| 基础 / 缠K | 2 | 逐字复制默认值 | 无意义 |
| 笔 | 7 | 逐字复制默认值 | 无意义 |
| 线段 | 6 | 逐字复制默认值 | 无意义 |
| 分析开关 | 5 | 逐字复制默认值 | 无意义 |
| 指标参数 | 24 | 仅 MACD 改为 12/26/9 | **唯一有效改动** |
| 推送/显示 | 15 | `图表展示/推送*=False` | 有效（静默模式） |
| 买卖点 | 8 | 逐字复制默认值 | 无意义 |
| 背驰 | 4 | 逐字复制默认值 | 无意义 |
| 其他 | 2 | 空值 | 无意义 |

**`config.py` 共 160 行，实质只做了 2 件事**：MACD 参数改标准值、关掉推送。其余是与核心默认值一字不差的复制。

### 配置开关的实际影响（实测）

| 配置 | 分型 | 笔 | 笔中枢 | 线段 | 扩展线段 | 官方买卖意义命中 |
|---|---|---|---|---|---|---|
| 默认 | 31 | 30 | 3 | 5 | 10 | 18 |
| `分析扩展线段=False` | 31 | 30 | 3 | 5 | **0** | 18 |
| `买卖点激进识别=True` | 31 | 30 | 3 | 5 | 10 | 18 |
| `笔内元素数量=3` | **87** | **86** | **10** | **13** | **28** | **39** |
| `指标计算方式='高'` | 31 | 30 | 3 | 5 | 10 | **21** |

**`笔内元素数量` 是最强杠杆**：从默认 5 改成 3，笔数 30 → 86（×2.9），扩展线段 10 → 28。项目把它钉死在默认值 5，等于放弃了成笔灵敏度的调优空间。

`指标计算方式='高'` 会同时改变 MACD 数值（DIF −0.23418 → −0.23206）和买卖意义命中数（18 → 21）。

---

## 8. Skill 功能覆盖评估

### 8.1 覆盖矩阵

| Skill 声称的能力（SKILL.md / usage-scenarios.md） | 实现状态 | 证据 |
|---|---|---|
| 结构分析（分型/笔/线段/中枢） | **已实现，仍可扩展** | 已输出线段内部笔中枢、扩展序列组和多级别展开；混合层仍需继续审计 |
| 标准买卖点（一/二/三类） | **已实现** | 方向已按官方语义修正，结果进入标准信号契约 |
| T 系列买卖点 | **已实现** | 已实现 T1/T1P/T2/T2S/T3A/T3B 分类 |
| 背驰判断 | **已实现，证据化** | 输出线段内部、笔内及 MACD/斜率/测度组合证据矩阵 |
| 技术指标（MACD/RSI/KDJ） | **已实现** | 指标从核心 K 线对象读取，并支持 BOLL/均线扩展 |
| 多周期分析 | **已实现** | 批量模式分别投喂观察者，避免单周期构造限制 |
| 共振分析 | **已实现** | 基于多周期同向信号和时间窗口输出共振事件 |
| 线段级分析 | **已实现** | 当前线段、线段内部中枢、背驰和递归层均输出 |
| 止损/失效位 | **已实现** | 接入买卖点工厂的破位值、失效 K 线和有效性 |
| 扩展/混合级别结构 | **部分实现** | 扩展线段/中枢序列组已展开；混合层和更完整级别联立仍可增强 |

**早期综合覆盖率约 27% 已失效**。当前实现已覆盖主要声称能力，但仍建议用离线真实样本继续验证递归级别、混合扩展和实时增量一致性。

### 8.2 文档与实现的三处脱节

1. **`agents/openai.yaml` 的 usage 名与真实 API 一致**（`analyze_stock`/`get_signals`/…），但 `SKILL.md` 正文与两个脚本用的是错名 —— 说明 yaml 对着代码写，SKILL.md 凭记忆写。
2. **`examples/usage-scenarios.md` 的示例输出无法复现**：场景 5 写「斜率背驰：是」，实测恒为 False；场景 1/4 中的具体点位与仓位建议，在当前实现下无对应产出。
3. **场景 4 要求输出「仓位管理：首次买入 30%（3万元）」**，与 SKILL.md 自身的「避免确定性荐股 / 不替用户决定具体投资金额」纪律**直接冲突**。

---

## 9. 缺陷清单（按严重度）

### P0 — 结果错误，必须修

| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| P0-1 | `get_signals()` / `get_segment_signals()` 买卖方向映射反向 | 18/18 样本与官方语义相反 | **所有买卖点输出都是错的**，AI 会据此做反向判断 |
| P0-2 | 自研信号在同一笔上自相矛盾 | 笔#2 同时输出「一卖」和「二买」；笔#3 同时输出「一买」和「二卖」 | 信号不可信 |
| P0-3 | 单周期构造触发 Rust `PanicException` | `["day"]` 直接崩，且 `except Exception` 抓不住 | 进程猝死，无堆栈可查 |
| P0-4 | 中文章期参数不兼容 | `periods=["日线"]` → `ValueError`；`--freq` 默认值即崩溃 | SKILL.md 推荐命令 exit 1 |

### P1 — 历史能力缺口（多数已完成，保留作复核清单）

| # | 问题 | 证据 |
|---|---|---|
| P1-1 | 技术指标功能未实现 | **已完成**：从核心 K 线对象读取指标，并有回归覆盖 |
| P1-2 | 明细取数字段错误 | **已完成**：端点、中枢边界和数据质量检查已对齐核心字段 |
| P1-3 | 结构覆盖 6/22 序列 | **部分完成**：扩展级别已输出，混合级别和完整级别联立仍可扩展 |
| P1-4 | 线段级分析是空壳 | **已完成**：当前线段、线段内部中枢、背驰和递归层已输出 |
| P1-5 | 买卖点数量失真 | **已完成**：改为结构优先的 T 系列分类和标准信号契约 |

### P2 — 工程问题

| # | 问题 |
|---|---|
| P2-1 | `tests/` 目录为空；根目录 5 个 `test_*.py` 是打印脚本、无断言 |
| P2-2 | `dist/` 混存 0.1.0 与 1.0.0，且内嵌一份 `skills/` 全量拷贝 |
| P2-3 | `site-packages/chanlun_skill` 是实体拷贝，改源码不生效 |
| P2-4 | `scripts/` 的 `sys.path` hack 少一层（`..'×2` 指向 `skills/` 而非仓库根），实际从 site-packages 导入 |
| P2-5 | `requirements.txt` 只声明 `chanlun eltdx`，但脚本 import `chanlun_skill` —— **未声明依赖** |
| P2-6 | `_seconds_to_name()` 缺 week/month 映射，周月线显示为 `604800s` |
| P2-7 | `except Exception: pass` 广泛使用，静默吞掉 AttributeError |
| P2-8 | `test_data.csv` 仅 58 行，实测 `分型=1 笔=0 中枢=0` —— **样例数据不足以生成任何结构** |

---

## 10. 优化建议

### 阶段一：止血（修正确性，不动架构）

1. **修买卖点方向**。两种路线：
   - **推荐**：废弃 `identify_standard_signals()` 的 120 行启发式，改用官方 `虚线.买卖意义` + 明确的买卖判定规则（如 `笔.武.中.标的K线.macd.MACD柱` 符号，或 `分型.与MACD柱子分型匹配`）。
   - 保守：先只把 `is_buy = "向上" in direction` 反转为 `is_buy = "向下" in direction`，并补一条回归测试。
2. **修单周期 panic**：`periods=[freq]` → 自动补一个更大周期（day→week，5m→day），并捕获 `BaseException` 而非 `Exception`。
3. **修中文章期**：在 `--freq` 入口统一映射，或直接用英文 choices。
4. **修取数字段**：`笔.文/武.分型特征值` + `.时间戳`；`中枢.高/低`。
5. **修样例数据**：`test_data.csv` 扩到 ≥200 根，否则一切输出都是空的。

### 阶段二：对齐官方语义（能力补齐）

6. **技术指标**：从 `K线.macd/rsi/kdj/指标` 取真值，替掉 `get_indicators()` 的配置回显。
7. **背驰**：改用 `_OBS` 变体；补 `线段.判断线段内部是否背驰` / `线段.四象` / `线段.特征序列状态`。
8. **结构覆盖**：补 `中枢序列`（线段中枢）、`扩展线段序列`、`扩展中枢序列`、`扩展线段序列组`。
9. **止损体系**：接入 `买卖点工厂` 的 `破位值` / `失效K线` / `有效性`，这是 SKILL.md 要求的「风险优先」目前完全缺失的一环。
10. **报告层**：保留 `text.py` 的排版骨架（跨周期遍历/命名/分段/截尾/汇总表），取数换成官方正确字段，输出升级为结构化 JSON。

### 阶段三：能力扩展（发挥核心库上限）

11. **T 系列买卖点**：18 种类型全部接入。
12. **参数敏感性**：把 `笔内元素数量`、`指标计算方式`、`买卖点_指标模式` 做成 CLI 参数 —— 实测它们对结果影响巨大（笔数 30↔86）。
13. **扩展级别分析**：利用 `扩展线段序列组=[10,3,1]` 做真正的多级别联立，而非只取第 1 层。
14. **`K线.获取MACD(序列,始,终)`** 的 阴/阳/总/合 用于量化背驰强度。
15. **共振分析**：基于官方买卖点类型做跨周期共振判定（当前仅有文字描述）。

### 附：应立刻写入项目 CLAUDE.md / 记忆的硬约束

```
- 立体分析器：周期组必须 ≥2；只能投喂最小周期；PanicException 继承 BaseException，须用 except BaseException
- 笔/线段的深度方法是 classmethod：线段.四象(seg) 而非 seg.四象()
- 中枢.完整性(虚实="合") —— 参数是字符串，取值 "合"/"实"/"虚"
- 分型.时间戳 ≠ 分型.中.标的K线.时间戳，两者不是同一个时间
- 买卖意义 返回 (是否具备买卖意义, 原因)，不含买/卖方向；官方约定：买在 MACD 负、卖在 MACD 正
```

---

## 附：实测环境与数据说明

- 合成数据：600 根日线 + 引擎自动合成 86 根周线；生成器含多尺度正弦漂移 + 随机波动，确保产生足够的分型/笔/线段/扩展结构。
- 真实数据：`skills/chanlun-skill/scripts/test_data.csv`（58 行，2024-01-02 ~ 2024-03-29）。
- 所有数字均来自实际运行输出，未做估算。标注「需进一步验证」的项为单一数据集结论。
