---
name: chanlun-skill
description: 基于 chanlun 库（缠论分析引擎）的 A 股缠论分析技能，支持多周期结构分析、买卖点识别、背驰检测与技术指标。当用户提供股票代码或 K 线数据并需要缠论结构研判、买卖点信号、背驰判断时使用。适用于 1 分钟到月线各周期；不替代持牌投资顾问，不凭空生成实时行情或确定性收益承诺。
---

# 缠论分析技能

## 概述

本技能基于 `chanlun==2606.73`（缠论分析引擎，MIT 许可，YuYuKunKun）提供 A 股市场的缠论分析能力。可选搭配 `eltdx==3.2.2`（通达信协议数据源）在线取数。

**核心架构**：Rust 核心库产出结构化缠论数据 → Skill 语义层整理事实/证据 → AI 按缠论规则验证分析 → 生成条件化策略报告。

当前 `chanlun==2606.73` 是 **Rust 核心 + PyO3 Python 绑定**，不是纯 Python 实现。顶层
`from chanlun import ...` 默认使用 Rust 对象；包内 `chanlun.chan` 是 Python 兼容/辅助实现，
用于阅读、调试和回归验证，不应与 Rust 对象混用在同一条分析链中。

> 关键事实：本 Skill 的生产路径是 Rust-first。`scripts/rust_adapter.py` 集中处理 Rust
> 绑定导入和观察者访问，`data_quality.py` 输出输入质量元数据，`semantic.py` 将结构结果
> 转成 AI 可消费的“事实—解释—情景”摘要。原始结构仍保留在 JSON 中。

## AI 解读协议

读取 JSON 时必须按“大级别 → 当前级别 → 背驰 → 买卖点 → 策略”的顺序：

1. 先判断各周期走势方向和走势类型；
2. 再读取当前线段、当前中枢及多级别展开；
3. 区分核心库直接检测的背驰证据矩阵、核心买卖意义/第三买卖线与 Skill 的买卖点分类；
4. 将结论标记为**事实**、**推断**或**策略**；
5. 策略必须给出触发条件、确认条件、仓位建议、止损、失效条件和备选情景。

不得把未确认结构写成已确认信号，不得把买卖点解读为确定收益或必然涨跌。
默认优先使用 `语义摘要`；只有需要审计或复核时才展开完整原始序列。

## 三大核心理念

1. **不测而测** —— 不预测市场未来，只分析当下正在发生什么
2. **完全分类** —— 事先列出所有可能情况，为每种情况准备应对策略
3. **级别思维** —— 任何分析都必须明确级别，不同级别有不同操作策略

## 操作原则

- **生存第一**：永远不要让一次失败毁掉所有
- **机械操作**：严格按系统操作，不加入个人情绪
- **节奏把握**：识别底、顶及中间过程，分阶段采取不同策略

---

## 核心库能力速查（务必对照使用）

### 立体分析器（多周期）

```python
from chanlun import 立体分析器, 缠论配置, K线

config = 缠论配置.不推送()          # 静默配置，不推送
engine = 立体分析器("sz000001", [86400, 604800], config)   # 周期组长度必须 ≥2

# 批量分析（本脚本实际采用）：分别对每个周期的观察者直接投喂，无 pending
obs_day = engine._单体分析器[86400]
obs_day.增加原始K线(K线.创建普K("sz000001", ts, o, h, l, c, v, i, 86400))

# 实时流式（可选，勿用于批量）：只投喂最小周期，引擎自动合成更大周期，
# 但最后一根 K 线会留在 pending 状态不进序列（需下一根触发 flush）
engine.投喂K线(K线.创建普K("sz000001", ts, o, h, l, c, v, i, 86400))
```

**必须遵守的硬约束：**

1. **周期组长度 ≥ 2**。单周期（如 `[86400]`）会让引擎抛出 `PanicException`（`index out of bounds`）。该异常继承 `BaseException`，**普通 `except Exception` 抓不到**，脚本会无声崩溃。
2. **批量分析用 `观察者.增加原始K线`，不要用 `投喂K线`**。`投喂K线` 是增量式合成器（实时流式场景），会遗留最后一根 pending；且若声明 `[1800, 86400]` 却投喂 86400s 的 K 线会抛 `PanicException: 周期不匹配`。批量分析直接对各周期观察者分别 `增加原始K线` 即可。
3. **周期秒数对照**：60(1m)/300(5m)/900(15m)/1800(30m)/3600(60m)/86400(day)/604800(week)/2592000(month)。
4. **month 是最大周期，无法向上补足**。单独分析 month 时，周期组用 `[604800, 2592000]`（week 作占位留空）。用 `增加原始K线` 直接投喂无此问题；若用 `投喂K线` 则须注意它会校验周期==周期组最小值。**注意 `观察者.投喂原始数据` 需要 5 个位置参数（开高低收量），投单根 K 线要用 `增加原始K线`。**
5. **`周期组` 是属性不是方法**（`engine.周期组` 直接返回 list，调用 `周期组()` 抛 `TypeError`）。`立体分析器` 公开面只有 `周期组`/`投喂K线`/`测试_保存数据` 三个成员，**没有公开取观察者的方法**，取观察者只能走私有字段 `engine._单体分析器[秒数]`。

### 观察者（结构序列）

每个周期的 `观察者` 暴露 22 个结构序列，全部是只读属性：

| 属性 | 含义 |
|---|---|
| `普通K线序列` / `缠论K线序列` / `基础缠K序列` | K 线 |
| `分型序列` | 分型 |
| `笔序列` / `笔_中枢序列` | 笔、原始全局笔中枢（仅底层审计） |
| `线段序列` / `中枢序列` | 线段、线段中枢 |
| `扩展线段序列` / `扩展中枢序列` | 扩展线段、扩展中枢 |
| `扩展线段序列_线段` / `扩展中枢序列_线段` | 线段级扩展 |
| `线段_线段序列` / `线段_中枢序列` | 线段之线段 |
| `扩展线段序列_扩展线段` / `扩展中枢序列_扩展线段` | 扩展线段之扩展线段 |
| `线段序列组` / `中枢序列组` / `扩展线段序列组` / `扩展中枢序列组` / `混合扩展线段序列组` / `混合扩展中枢序列组` | 按层次分组的序列组（多级别递归结构） |

> 买卖点结构判断使用每条线段内部的 `seg.合_中枢序列`（线段内部笔中枢）。
> 原始全局 `obs.笔_中枢序列` 不参与一/二/三类买卖点分类，不参与走势判据；
> 报告中的“笔中枢”默认指“线段内部合中枢”。

### 对象字段（取数直接读这些，不要二次推断）

- **笔/线段（`虚线`）**：`.高` `.低` `.方向` `.序号` `.级别` `.文`(起点分型) `.武`(终点分型) `.获取数据文本()` `.图表标题`
- **分型**：`.时间戳` `.分型特征值` `.结构`(顶/底) `.左/.中/.右`
- **中枢**：`.高` `.低` `.高高` `.低低` `.第三买卖线` `.当前状态()` `.完整性(虚实)` `.获取数据文本()`
- **缠论K线**：`.高` `.低` `.分型` `.分型特征值` `.买卖点信息` `.标的K线`

> 注意：`分型.时间戳` 与 `分型.中.标的K线.时间戳` 不是同一个值；`中枢.完整性()` 的参数是字符串 `"实"/"虚"/"合"`，不是布尔。

### 买卖点

**一类结构标准**：

- 趋势一类：`a+A+b+B+c`，`A/B` 是该笔之前已经形成且仍有效的两个依次同向且不重叠的线段内部笔中枢，
  `c` 是离开第二个中枢后的末段。
- 盘整一类候选：`a+A+b`，`A` 是该笔之前已经形成且仍有效的一个线段内部笔中枢，`b` 是其后的末段。
- “已形成”指核心中枢基础序列已达到至少三条有效重叠虚线；“仍有效”指该中枢仍在核心
  的有效中枢序列中。核心 `完整性("实")` 是第三买卖线/内部离开等更严格的辅助证据，
  **不是 A/B 的硬门槛**；因此已形成但 `完整性("实") == False` 的中枢仍可作为 A/B。
- MACD、斜率、测度、线段内部背驰、笔内背驰等都只是 `c`/`b` 的辅助确认依据；
  任意背驰不能单独定义一买/一卖。

**`虚线.买卖意义(线, 观察者) -> (bool, str)`** 是核心的买卖点判据，但语义要分清：

- 只返回「此处是否具备买卖意义」和理由（如 `"背驰过:0,极值:True,柱子分型匹配"`）
- **不返回买/卖方向，不返回买卖点类型**
- **方向判定**：向上笔终点是顶分型 → 卖出语境；向下笔终点是底分型 → 买入语境
- 配置注释 `买卖点_指标匹配_MACD: bool = True  # 买在负，卖在正！` 是官方判据：MACD 柱为负是买点、为正对应卖点

**`买卖点类型` 枚举共 18 种**：`一买/一卖/二买/二卖/三买/三卖` + 12 种 T 系列（`T1买`…`T3B卖`），均带 `是买点`/`是卖点` 布尔属性。

**`买卖点` 工厂方法**（可生成带止损信息的完整买卖点对象）：`买卖点.一买点(分型, 当前K线, 标识, 备注, 中枢破位值)`，返回对象含 `.破位值` `.失效K线` `.有效性` `.与MACD柱子匹配`。

### 背驰

`背驰分析` 共 14 个方法，`线段`/`笔` 另有深度方法。**全部是 classmethod，实例必须作为第一个参数传入**：

```python
from chanlun import 背驰分析, 线段

线段.判断线段内部是否背驰(段, 观察者)      # 正确
# 段.判断线段内部是否背驰(观察者)           # 错误：会抛 AttributeError

背驰分析.MACD背驰_OBS(进段, 出段, 观察者)   # MACD 背驰
背驰分析.斜率背驰(进段, 出段)               # 斜率背驰
背驰分析.测度背驰(进段, 出段)               # 测度背驰
背驰分析.全量背驰_OBS(进段, 出段, 观察者)    # 全量（MACD+斜率+测度）
线段.是否背驰过(段, 观察者)                 # 返回背驰过的缠K列表
```

### 技术指标（挂载在每根 K 线上）

指标**不是**由 `观察者` 单独返回，而是挂在每根 K 线上：

```python
k.macd   # 平滑异同移动平均线：.DIF .DEA .MACD柱 .快线EMA .慢线EMA
k.rsi    # 相对强弱指数：.RSI .平均上涨 .平均下跌
k.kdj    # 随机指标：.K .D .J .RSV
k.指标   # 指标容器（可含 BOLL 等）
```

另有 `K线.获取MACD(序列, 始, 终) -> {'阴','阳','总','合'}`，正是量化背驰所需的面积量。

### 缠论配置（74 个字段）

`缠论配置(**kwargs)`，字段名即中文参数名。常用杠杆：

| 字段 | 默认 | 说明 |
|---|---|---|
| `笔内元素数量` | 5 | 成笔最低长度，改 3 笔数可 ×2.9 |
| `买卖点激进识别` | False | 激进模式不考虑分型完整性 |
| `买卖点偏移` | 1 | 买卖点最大偏移 |
| `买卖点_指标匹配_MACD` | True | 买在负、卖在正 |
| `线段内部背驰_MACD/斜率/测度` | True | 内部背驰开关 |
| `计算指标` | True | 是否计算 MACD/RSI/KDJ |
| `MACD_参数列表` / `RSI_周期列表` / `KDJ_参数列表` | None | 多参数指标 |

`config.不推送()` 生成静默配置；`config.to_dict()` 序列化。

---

## 分析思维流程

1. **明确级别**：周线/日线/30m/5m？操作级别是什么？
2. **识别结构**：上涨/下跌/盘整？中枢在哪？当前位置？
3. **判断买卖点**：第几类买卖点？有无背驰确认？
4. **评估风险收益**：止损位（`破位值`）、目标位（中枢上下沿）
5. **完全分类**：列出所有可能情形及应对策略

## 数据与表达纪律

1. **先确认数据**：明确标的、时间范围、周期、复权、数据源。缺数据先说明限制，不编造。
2. **区分事实与判断**：先列可观察的结构（K线/分型/笔/线段/中枢/指标），再给缠论解释；标注不确定性。
3. **避免确定性荐股**：输出教育性分析和条件化预案，不承诺收益。
4. **风险优先**：任何策略包含失效条件、止损/退出、仓位约束、等待方案。
5. **当前行情要验证**：涉及实时行情必须核验数据后再分析。

---

## 脚本工具

### chan_analyzer.py（唯一分析入口）

`chan_analyzer.py` 是主入口；配套 `rust_adapter.py`、`data_quality.py`、`semantic.py`
分别负责 Rust 绑定适配、输入质量检查和 AI 语义摘要。

```bash
# 本地 CSV 分析（离线可复现，推荐）
python scripts/chan_analyzer.py \
    --source csv --input scripts/test_data.csv --symbol 000001 --freq day

# 中文周期名等价
python scripts/chan_analyzer.py \
    --source csv --input scripts/test_data.csv --symbol 000001 --freq 日线

# 结构化 JSON 输出（便于 AI 消费）
python scripts/chan_analyzer.py \
    --source csv --input scripts/test_data.csv --symbol 000001 --freq day --json

# 多周期 CSV 输入（用于本地跨周期共振）
python scripts/chan_analyzer.py \
    --source csv \
    --input_periods day=scripts/day.csv,week=scripts/week.csv \
    --symbol 000001 --json

# 在线数据（eltdx 3.2.2，通达信 7709 协议，需网络连通）
python scripts/chan_analyzer.py \
    --source eltdx --code sh600519 --freq day --count 2000

# 如需切回未复权或后复权
python scripts/chan_analyzer.py \
    --source eltdx --code sh600519 --freq day --adjust none

# 输出到文件
python scripts/chan_analyzer.py \
    --source csv --input scripts/test_data.csv --symbol 000001 --freq day --output report.txt
```

> eltdx 模式 `--code` 即标的（如 sh600519 / sz000001 / 000001），分析标题自动用 code；`--symbol` 可覆盖。周期 `--freq` 支持 1m/5m/15m/30m/60m/day/week/month。

`eltdx` 单次 K 线请求最多 800 根，但 `--count` 表示本次分析需要的总数。
当 `--count > 800` 时，取数层会自动使用 `start` 游标分页（默认每页 800 根），
合并后去重并按时间升序排列，再一次性投喂 Rust 核心，避免分页边界切断笔、线段或中枢。
可用 `--page-size 1~800` 调整单页大小，`--max-pages` 限制最大分页次数；分页异常、
重复页和空页都会安全停止或给出明确错误。日期范围在合并后裁剪。

复权模式默认 `--adjust qfq`（前复权），用于保持历史走势连续，降低除权缺口对笔、
线段、中枢和背驰判断的干扰。可显式使用 `--adjust none` 对齐未复权真实价格，
或使用 `hfq/fixed_qfq/fixed_hfq`；定点复权必须同时提供 `--anchor-date YYYY-MM-DD`。
JSON 输出会在 `data_source.复权模式` 中记录本次复权口径。

**脚本输出内容**（每个周期）：
- 结构计数：K线/缠K/分型/笔/笔中枢（线段内部合中枢）/线段/中枢/扩展线段/扩展中枢/线段_线段/扩展线段_扩展线段；原始全局笔中枢仅作审计计数
- 数据质量：K线数量、有效行、时间解析、重复/乱序/大间隔和 OHLC 合法性
- 语义摘要：事实、解释、情景；默认供 AI 解读使用，原始结构用于复核
- 标准信号：`标准信号` 统一类型、方向、确认状态、来源、证据、止损和可执行性
- 标准信号校验：`标准信号校验` 由独立 `scripts/signal_schema.py` 生成，校验字段、类型、枚举和可执行规则
- 策略计划：`策略计划` 明确观望/等待确认/条件执行，不使用未来数据回填历史结论
- 当前状态：走势类型、当前线段、当前中枢
- 走势判据：固定使用线段内部笔中枢，输出中枢序列、上移/下移/重叠次数
- 核心买卖点信息：绑定是否直接在缠论K线上挂载买卖点对象；若不可用会明确说明
- 级别递归序列组（线段序列组/中枢序列组/扩展序列组/混合扩展序列组 逐层计数）
- 最近笔（序号/方向/端点时间/高低价）
- 线段内部笔中枢（所属线段/区间/极值/当前状态/基础笔序号/第三买卖线）
- **T 系列买卖点**（六类 = 走势类型 + 背驰信息对基础买卖点的精确化，含破位值/止损参考）
- 背驰证据矩阵（`线段内部背驰` + `笔内背驰` + 相邻线段对 MACD/斜率/测度/全量/任意/任选/配置/模式）
- MACD 柱面积量（`K线.获取MACD` 全序列 阳/阴/合/总）
- 最近 3 根 K 线技术指标（MACD/RSI/KDJ + 可选 BOLL/均线）

**T 系列买卖点判据**（先满足缠论结构，再用背驰/指标作辅助证据）：

| 类型 | 基础 | 细分判据 |
|---|---|---|
| T1 | 一类 | 趋势结构 `a+A+b+B+c`：两个已形成且有效、依次同向且不重叠的中枢，末段 `c` 位于第二个中枢之后；背驰是 `c` 的辅助确认 |
| T1P | 盘整中的一类候选 | 盘整结构 `a+A+b`：一个已形成且有效中枢后的末段 `b`；背驰是 `b` 的辅助确认 |
| T2 / T2S | 二类 | 已出现同方向一类后，第一次回踩/反抽不破 = T2；后续同类机会 = T2S |
| T3A / T3B | 三类（`中枢.第三买卖线` 非空） | 中枢在一类之后 = T3A（新中枢突破）；在一类之前 = T3B（老中枢突破，即二三类重合） |

一类的结构门槛优先于背驰：`线段内部背驰`、`笔内背驰`、MACD/斜率/测度/全量等
所有背驰结果都只能进入 `背驰辅助证据` 或 `背驰证据矩阵`，不能单独升级为一买/一卖。
不满足 `a+A+b+B+c` 或 `a+A+b` 的背驰，只报告为背驰，不生成 T1/T1P。

**每个信号附带的止损字段**（`止损` 子对象，由 `买卖点` factory 构造）：

买卖点信号同时包含来源拆分字段：

- `结构来源=rust_core`：笔、线段、中枢、第三买卖线等结构事实；
- `类型来源=skill_classifier`：T1/T1P/T2/T2S/T3A/T3B 的二次分类；
- `止损来源=rust_factory`：买卖点 factory 提供的止损与有效性字段；
- `核心匹配`：Rust 绑定提供的配置/任意/全量/相对指标匹配结果；
- `确认级别=候选`：当前输出不是后验确认，需结合后续 K 线复核。

`核心买卖点信息` 用于审计绑定是否直接挂载官方买卖点对象。当前版本若该字段 `可用=false`，
不得把 Skill 分类标签描述成 Rust 原生买卖点序列。

`确认状态` 使用严格状态机：`候选 → 已确认`、`候选 → 已失效`、`已确认 → 已失效`；
已失效信号不可回到候选或已确认。批量历史分析不使用未来 K 线反填确认，因此默认保持
`候选`；只有上游明确提供确认状态时才会变为 `已确认`。`可执行=true` 的必要条件是
状态为 `已确认` 且止损工厂仍判定有效。每个信号的 `状态轨迹` 保留状态变化原因，便于审计。

`走势判据` 固定使用线段内部笔中枢（每条线段的 `合_中枢序列`），记录相邻中枢的上移、
下移和重叠关系。它是 Skill 的解释层判据，不等同于核心库直接返回的官方走势类型；
原始全局 `obs.笔_中枢序列` 只用于底层审计，不作为买卖点结构来源。

| 字段 | 含义 |
|---|---|
| `破位值` | 工厂根据输入计算的失效边界（中枢沿/端点近似） |
| `失效K线` | factory 判定的失效 K 线（若有） |
| `有效性` | factory 判定当前是否仍有效（已被突破则 False） |
| `失效偏移` | 距失效的 K 线根数 |
| `与MACD柱子匹配` | factory 判定的 MACD 柱正负是否匹配（买在负/卖在正） |
| `与MACD柱子分型匹配` | MACD 柱分型是否与买卖点分型匹配（更严格） |

factory 命名规则：`信号.kind + "点"` 直接命中 18 个 classmethod（如 `T1买` → `买卖点.T1买点`）。

**背驰证据矩阵**（`背驰[].证据矩阵`）：

相邻线段对会输出：

| 字段 | 含义 |
|---|---|
| `原子判据` | `MACD` / `斜率` / `测度` 三个基础判据 |
| `MACD面积方式` | `总` / `阳` / `阴` / `合` 四种 MACD 面积口径 |
| `组合判据` | `全量` / `任意` / `任选` / `配置` |
| `模式判据` | `背驰模式_OBS` 下的 `全量` / `任意` / `配置` / `相对` |
| `命中` | 命中的原子、组合和模式判据列表 |
| `强度` | 依据原子判据命中数给出 `强` / `中` / `弱` / `无` |

笔内/线段内部背驰会输出 `背驰K线数量` 和最多 5 个 `背驰位置`。AI 解读时应优先使用
`证据矩阵` 说明“哪个判据成立”，不要只说“有背驰”。

**跨周期共振**（`result["跨周期共振"]`，需多周期数据）：
- 主周期（小）的每个信号，在 30 天时间窗口内查找其他周期的同向信号
- 至少 2 个周期同向才算共振事件，强度 = 参与周期数
- 输出含 `primary_time` / `direction` / `strength` / `matches`（含距离天数）

**多级别展开**（`detail["多级别展开"]`，审计 Stage 3-13）：
- 把 `扩展线段序列组=[n₁,n₂,n₃]` 逐层展开为 L1/L2/L3
- 每层含层级、数量、明细（序号/方向/端点/起讫时间）
- 这是缠论「级别递归」的载体，多级别联立分析的基础

**可选参数**（配置杠杆）：
```bash
--笔内元素数量 3              # 成笔最低K线数（默认5，改3笔数约×2.9）
--买卖点激进识别              # 不考虑分型完整性
--boll                       # 计算布林带
--均线 5,20,60               # 计算均线
--均线类型 SMA,EMA           # 均线类型（默认 SMA，可多类型）
--指标计算方式 高              # 指标计算基准：收/高/低（影响 MACD 数值与买卖意义命中数）
--买卖点_指标模式 全量         # 买卖点指标匹配：全量/任意/配置
--买卖点_指标匹配_MACD True   # 是否要求买卖点与 MACD 柱分型匹配（True=严格）
--adjust qfq                  # eltdx 复权模式，默认 qfq 前复权；可选 none/hfq/fixed_qfq/fixed_hfq
--anchor-date 2024-06-03      # fixed_qfq/fixed_hfq 的锚定日期
```

**关键设计**：
- `--freq` 自动补上级周期（如 `day` → `[day, week]`，eltdx 分别取各周期真实数据），规避单周期 panic
- eltdx 取数支持超过 800 根的分页合并（`--count` 总数、`--page-size` 页大小、`--max-pages` 安全上限），
  页面不会分别分析，所有原始 K 线合并后才进入 Rust 递归
- eltdx 在线结构分析默认使用前复权 `qfq`，并在输出元数据中记录复权口径；需要与未复权现价、
  止损价逐笔对齐时显式传 `--adjust none`
- CSV 默认读取单周期；需要本地跨周期共振时使用 `--input_periods day=day.csv,week=week.csv`
- 数据加载后**直接对观察者 `增加原始K线` 批量投喂**，不走 `立体分析器.投喂K线` 的增量式合成器（那会遗留「最后一根 pending 不进序列」，且是实时流式场景才需要的设计）
- 周期名支持中英文（1m/5m/…/day/week/month ↔ 1分钟/日线/周线/月线）
- 取数全部走核心库原生字段（`.高/.低/.分型特征值`），不做二次推断
- 三买卖点用线段内部笔中枢上核心库已算好的 `中枢.第三买卖线`；一类先验证 `a+A+b+B+c` /
  `a+A+b` 结构，再把 `买卖意义`、笔/线段内部背驰和背驰证据矩阵作为辅助确认；
  二类只在同方向一类之后按回踩次序初筛。严格缠论的一二类还需结合操作级别和
  多级别递归，本输出仍是结构化初筛而非最终结论。
- 止损字段走官方 `买卖点` factory（`缠论K线.买卖点信息` 在所有配置下实测返回空 set()，故不可用）
- 异常处理全程用 `except BaseException` 兜底 Rust `PanicException`（继承 BaseException，普通 `except Exception` 抓不到）

> 脚本只用于复现结构计算和整理输入数据，不能替代对级别、数据质量和交易风险的判断。`eltdx` 官方仅授权个人学习与非商业研究使用。

**固定运行环境**：Python `3.14.7`；依赖通过 `scripts/requirements.txt` 固定为
`chanlun==2606.73`、`eltdx==3.2.2`、`PyYAML==6.0.3`。升级任一版本后应重新运行
契约测试、黄金样例和灰度测试。

**依赖自检与安装**：Claude Code/Codex 安装 Skill 时不会自动执行
`requirements.txt`。首次使用或切换环境时先运行：

```bash
python scripts/check_dependencies.py --json
```

自检默认只读，检查 Python 版本、发行版版本和关键模块导入；缺失或版本不符时退出码为 1。
在明确允许安装依赖后运行 `python scripts/check_dependencies.py --install`，脚本使用当前
解释器调用 pip 安装固定清单，并在安装后重新检查。Windows 中文终端可设置
`PYTHONUTF8=1`。

**灰度测试**：覆盖 16 个 PART（功能/边界/数据/方向/T 系列/趋势/T3A-B/止损/多级别/共振/CLI 杠杆/性能/信号溯源/黄金回归/schema、状态机与 eltdx 分页取数）。`python gray_test.py` 退出码 0 = 全部通过。

## 参考资源

- [缠论核心理论](references/chan-theory-core.md) —— 完整理论体系与金句
- [使用场景示例](examples/usage-scenarios.md) —— 实战案例（注意：示例中的「斜率背驰」等字段名是旧版虚构，以本 SKILL.md 的 API 速查为准）

## 理论来源

本技能基于缠中说禅 2006–2008 年《教你炒股票》系列 108 课思想整理，主要参考 chanlun 库实现。
